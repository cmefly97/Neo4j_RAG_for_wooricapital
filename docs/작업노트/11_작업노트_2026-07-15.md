# 작업 노트 — 2026-07-15

GitHub 저장소 최초 공개(푸시)와 LLM/임베딩 접속 정보를 **전용 vLLM 서버**로 전환.
관련 설정 문서(`../06_Hybrid_구조화_가이드.md`, `README.md`, `.env.example`)도 함께 갱신.

## 오늘 한 일

### 1) GitHub 저장소 초기화 & 최초 푸시
- 저장소: `git@github.com:cmefly97/Neo4j_RAG_for_wooricapital.git` (branch `main`).
- 로컬에 SSH 키가 없어 **ed25519 키 신규 생성**(`~/.ssh/id_ed25519`) → GitHub 계정(`cmefly97`)에 공개키 등록 → SSH 인증 성공 후 푸시.
- 커밋 전 스테이징 감사: **`.env`(비밀값) 미포함** 확인, `.env.example`은 placeholder만 있어 포함. `source 복사본`(백업)·`.venv`·`logs` 등 제외.

### 2) LLM/임베딩 엔드포인트를 전용 vLLM 서버로 전환
- **Qwen**: `QWEN_BASE_URL=http://223.130.140.218:8000/v1/chat/completions`, `QWEN_MODEL=Qwen/Qwen3.6-35B-A3B`.
- **임베딩(bge-m3)**: `EMBED_BASE_URL=http://223.130.140.218:8001/v1`, `EMBED_MODEL=BAAI/bge-m3`, `EMBED_API_KEY=`(빈 값).
  - 처음엔 `211.188.49.10:8002`(Octen-Embedding-8B, **4096차원**)를 시도했으나 기존 벡터 인덱스(1024)와 불일치 → **bge-m3 전용 서버(`:8001`)로 변경**.
- 두 값 모두 `.env`(실제)·`.env.example`(플레이스홀더)에 반영.

### 3) 접속 테스트 (앱 코드 경로로 검증)
- `/v1/models`로 서버 실제 ID 확인 → **vLLM은 네임스페이스 접두어 필요**(`bge-m3` → 404, `BAAI/bge-m3` OK / `Qwen3.6-35B-A3B` → 404, `Qwen/Qwen3.6-35B-A3B` OK).
- **임베딩**: `Embedder.embed()` OK, **1024차원 → 기존 `EMBED_DIM=1024`와 일치 → 재인덱싱 불필요**.
- **Qwen**: `build_chat_model('qwen').invoke()` OK, 응답 정상(thinking 모델).
- 임베딩 서버는 **인증 불필요** → `EMBED_API_KEY` 빈 값이어도 동작(HCX 키 폴백은 서버가 무시).

### 4) 저장소 정리 — `.omc` 런타임 상태 추적 해제
- 초기 커밋에 섞여 들어간 `.omc/`(OMC 툴 세션/상태 json)가 매 세션 변경 노이즈 유발.
- `.gitignore`에 `**/.omc/` 추가 + `git rm --cached`로 전부 추적 해제(머신 로컬 상태, 버전관리 불필요).

### 5) 문서 갱신
- `README.md` — 저장소 주소, 전용 vLLM 서버(:8000/:8001) 안내, **vLLM 모델 ID 접두어 주의** 박스 추가.
- `06_Hybrid_구조화_가이드.md` — `.env` 임베딩 블록을 새 서버/모델 ID로 갱신 + 2026-07-15 전환 메모.

---

## 다음 작업 후보
- vLLM 서버 접속 정보 변동 시 `.env`만 갱신(문서의 예시는 `<host>` 유지).
- 다른 문서(`02_Phase1_PoC_설계서.md`)의 "사내 게이트웨이" 표기도 필요 시 전용 서버로 정정.
