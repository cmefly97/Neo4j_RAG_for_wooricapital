# 작업 노트 — 2026-07-27

로컬 실행(백엔드+프론트) 확인과 README/문서 최신화. 직전(07-21) LLM 설정 변경분 정리 반영.

## 오늘 한 일

### 1) 로컬 서비스 기동 확인
- 백엔드 `uvicorn app.main:app --port 8000` — `/models` → `["hcx30"]` (단일 모델) 정상.
- 프론트 `npm run dev` (`http://localhost:5173`) — HTTP 200. `src/api/client.ts`의 `BASE=http://localhost:8000`로 백엔드 호출.
- 브라우저(Playwright)로 사용자 탭 로드 확인(콘솔 에러 없음). 모델 표시는 드롭다운 제거 → 고정 라벨 `HCX-30B-Text (hcx-agent-06)`.

### 2) LLM 호출 옵션 확정 (직전 커밋 5336e42 반영 정리)
- `max_tokens=1000`, **thinking 비활성**(`chat_template_kwargs.thinking=false`). `app/llm/registry.py`의 hcx30 spec + `factory.py`가 `spec.max_tokens` 우선 적용.
- 실제 검색으로 검증: thinking 노출 없이 한국어 답변, 발췌 폴백 아님.

### 3) 설정 로딩 정책
- `app/config.py`는 하드코딩 기본값 없이 **`.env`에서만** 로드 → 누락 시 기동 즉시 실패(fail-fast).
- 접속 정보 현황: Neo4j `223.130.140.218:7687`, LLM(HCX-30B) `223.130.140.68:8000`(hcx-agent-06), 임베딩(bge-m3) `101.79.25.26:8001`(BAAI/bge-m3, 1024차원).

### 4) 문서 갱신
- `README.md` — LLM 호출 옵션(max_tokens/thinking)·`.env` 전용 설정 정책 추가.
- `.gitignore` — Playwright 산출물(`.playwright-mcp/`, `user-tab.png`, `page-*.png`) 제외.

---

## 참고 / 다음 작업 후보
- 벡터 vs Hybrid 비교의 `no_vector`는 임베딩 설정 누락 또는 `:Chunk(embedding)` VECTOR 인덱스 부재/비ONLINE이 원인 (현재 환경은 `is_ready()=True`, `chunkVec` 정상).
- `.env`는 gitignore 대상 — 접속 정보 변동 시 `.env`만 수정 후 백엔드 재기동.
