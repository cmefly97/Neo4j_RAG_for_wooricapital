# 작업 노트 — 2026-07-21

유일 운영 모델 **HCX-30B-Text**의 게이트웨이 엔드포인트가 교체되어 접속 정보를 갱신하고,
전체 검색 파이프라인(임베딩 + LLM 답변)이 정상 동작함을 확인.

## 오늘 한 일

### 1) HCX-30B-Text 엔드포인트 장애 진단
- 검색 시 `APIConnectionError: Connection error`로 답변이 `[문서 발췌]` 폴백 처리됨.
- 도달성 점검: 기존 `HCX30_BASE_URL=http://223.130.140.68:11000`이 **연결 거부(HTTP 000)**.
  같은 대역 `.218`(Neo4j·Qwen)·`101.79.25.26`(임베딩)은 정상 → 해당 호스트:포트만 다운/차단으로 확인.

### 2) HCX30 접속 정보 교체 (`.env`)
- 운영자로부터 새 엔드포인트 수령 → `.env` 갱신:
  - `HCX30_BASE_URL`: `...68:11000/...` → **`http://223.130.140.68:8000/v1/chat/completions`**
  - `HCX30_MODEL`: `hcx-agent-05` → **`hcx-agent-06`**
  - `HCX30_API_KEY`: 새 키(`poc-…`)로 교체. (`.env`는 gitignore라 커밋 제외)

### 3) 접속 테스트
- 직접 curl: `hcx-agent-06` + `thinking:true` → "서울" 정상 응답.
- 앱 검색(재기동, `model=hcx30`): `status=ok`, `model_used=HCX-30B-Text (hcx-agent-06)`, rows=8,
  한국어 답변 정상("론 상품의 취급 가능 기간은 12~72개월입니다. 출처: 9_샘플_중고승용 상품운영기준.pdf").
- 전체 파이프라인 확인: 임베딩(`101.79.25.26:8001`) + LLM(`223.130.140.68:8000`) → 발췌 폴백 없이 답변 생성.

### 4) 문서/설정 파일의 HCX-30B 정보 갱신
- `hcx-agent-05`→`hcx-agent-06`, 포트 `11000`→`8000` 반영:
  - 코드: `app/config.py`(기본값·예시 주석), `app/llm/registry.py`(docstring·주석).
  - 문서: `README.md`, `CLAUDE.md`, `docs/02_Phase1_PoC_설계서.md`, `docs/06_Hybrid_구조화_가이드.md`.
- **실제 API 키는 어떤 추적 파일에도 넣지 않음**(`.env`에만 보관).
- 과거 작업노트(06-30, 07-20)는 당시 기록이라 그대로 둠(이력 보존).

---

## 참고
- 엔드포인트/키가 또 바뀌면 `.env`의 `HCX30_*`만 수정 후 백엔드 재기동하면 됨.
- 문서에는 실제 키를 기록하지 않는다(호스트/포트/모델 ID까지만).
