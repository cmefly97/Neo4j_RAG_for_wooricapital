# 단위 테스트 계획 (Phase 1 PoC)

> 실행: `cd backend && pip install -r requirements.txt && pytest -v`
> 네트워크·Neo4j 불필요 (mock 사용). 현재 17개 테스트 전부 통과.

## 대상 함수 및 검증 항목

### 로깅 (`app/logging_setup.py`)
| 함수 | 검증 | 테스트 |
|---|---|---|
| `log_action` | 날짜별 파일(`logs/YYYY-MM-DD.log`)에 `ACTION ...` 라인 기록 | `test_log_action_writes_dated_file` |

### LLM 레지스트리 (`app/llm/registry.py`)
| 함수 | 검증 | 테스트 |
|---|---|---|
| `list_models` | 2개 모델, claude 기본 1개 | `test_list_models_has_two_and_default` |
| `get_model_spec` | None→기본, 잘못된 id→`ValueError` | `test_get_model_spec_default_and_invalid` |
| `_is_available` | 키 설정 여부 반영 | `test_is_available_reflects_keys` |

### LLM 팩토리 (`app/llm/factory.py`)
| 함수 | 검증 | 테스트 |
|---|---|---|
| `_anthropic_sdk_base` | 공인 URL→None, 사내 URL→host root | `test_anthropic_base_normalization` |
| `_openai_compat_base` | `/chat/completions` 제거 | `test_openai_compat_base_normalization` |

### 검색 (`app/search/cypher_qa.py`)
| 함수 | 검증 | 테스트 |
|---|---|---|
| `_extract_cypher` | 코드펜스/추론서두/평문에서 Cypher 추출 | `test_extract_cypher_fence_think_plain` |
| `_fallback_cypher` | 라벨 부분일치 쿼리 형태 | `test_fallback_cypher_contains_contains` |
| `search` (폴백) | LLM 미가용 → 폴백 + `(폴백)` 표기 | `test_search_fallback_path` |
| `search` (LLM 성공) | Cypher 생성→실행→답변 | `test_search_llm_success` |
| `search` (LLM 예외) | 예외 시 폴백 강등 | `test_search_llm_error_falls_back` |

### API (`app/api/*.py`, `app/main.py`)
| 엔드포인트 | 검증 | 테스트 |
|---|---|---|
| `GET /health` | 200 `{status: ok}` | `test_health` |
| `GET /models` | 모델 목록, claude 기본 | `test_models` |
| `POST /search` | 200 + `SEARCH_REQUEST`/`SEARCH_OK` 로그 | `test_search_ok_logs` |
| `POST /search` 예외 | 500 `{error}` + `SEARCH_ERROR` 로그 | `test_search_error_returns_500_and_logs` |
| `GET /admin/documents` | source_dir 파일 목록 | `test_documents_lists_files` |
| `POST /admin/upload` | 잘못된 확장자 → 400 | `test_upload_rejects_bad_ext` |

## 미커버(향후) — 실제 연동 필요
- `parsers.py`/`extract.py` (미구현)
- 실 Neo4j 적재 결과 검증(통합 테스트), 실 LLM 응답 품질(`tests/test_llm_connection.py`)
